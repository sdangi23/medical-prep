[project]
name = "ruhs-mo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shubhamd", email = "shubhamd@valuefy.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "jinja2>=3.1.6",
    "pandas>=3.0.6",
    "pyyaml>=6.0.3",
    "uvicorn>=0.53.0",
]

[project.scripts]
ruhs-mo = "ruhs_mo:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
